# Nome da aplicação
spring:
  application:
    name: GS_JAVA_SOLAR_SENSE

# Banco de dados
spring.datasource:
  url: jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL # Substitua `localhost`, `1521` e `xe` pelos valores corretos para seu ambiente
  username: rm551279
  password: 230704
  driver-class-name: oracle.jdbc.OracleDriver

# JPA / Hibernate
spring.jpa:
  # database-platform: org.hibernate.dialect.OracleDialect # Dialeto específico para Oracle
  hibernate:
    ddl-auto: update
  show-sql: true
  properties:
    hibernate:
      format_sql: true

# Produção (comentado)
# spring:
#   jpa:
#     hibernate:
#       ddl-auto: validate
#     show-sql: false
#     properties:
#       hibernate:
#         format_sql: false
